overwrite: true
schema: 'http://localhost:4000'
documents:
  - 'src/graphql/**/*.graphql'
generates:
  src/types/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'typescript-react-apollo'
